# PAUL Handoff

**Date:** 2026-04-15
**Status:** Paused — plan created, audited, ready for APPLY

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** FleetGo — Passenger coach fleet management system
**Core value:** Digitize and automate back-office operations (vehicles, routes, trips, ticketing) into a single source of truth for transportation companies.

---

## Current State

**Version:** v0.1 MVP (in progress)
**Phase:** 4 of 8 — Route & Station Management
**Plan:** 04-01 (Stations CRUD) — created + audited, awaiting APPLY

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ○        ○     [Plan audited, ready for APPLY]
```

---

## What Was Done This Session

- Ran `/paul:progress` — confirmed Phase 3 loop fully closed, Phase 4 is next
- Ran `/paul:plan` — created `04-01-PLAN.md` for Stations CRUD (standard track)
- Ran `/paul:audit` — enterprise audit complete:
  - **2 must-have fixes applied:**
    1. `mapSupabaseError` 23505 check was `includes('name')` (too generic) → changed to constraint-specific `stations_name_key` + `details.(name)` check
    2. `serializeFormDefaults(station)` was referenced but never defined → replaced with explicit inline `useEffect` reset mapping for both create (station===null) and edit (station!==null) modes
  - **5 strongly-recommended fixes applied:**
    1. Search trim before `.or()` ilike filter
    2. lat/lng Input must use `type="text"` (not `type="number"`) for `z.coerce.number()` pattern
    3. `is_active` Switch requires `<Controller>` (not `register()`)
    4. Edit→create mode reset explicitly covered by must-have #2
    5. Human-verify checkpoint now includes regression navigation steps + AC-8 delete-RESTRICT deferral note
  - Verdict: **Conditionally acceptable (now ready)**

---

## What's In Progress

- Nothing partially implemented — plan is complete and audited, no code written yet

---

## What's Next

**Immediate:** Run `/paul:apply .paul/phases/04-route-station-management/04-01-PLAN.md`
- Invoke `/feature-sliced-design` and `/frontend-design` skills first (required per plan's skills section)
- Task 1: Station entity slice (types + api + queries + barrel)
- Task 2: Station page + form dialog + delete dialog + router wiring
- Checkpoint: human-verify full CRUD flow at /stations

**After that:** Plan 04-02 (Routes CRUD) depends on `@entities/station` FK dropdowns

---

## Phase 4 Plan Overview (3 plans total)

| Plan | Name | Status |
|------|------|--------|
| 04-01 | Stations CRUD | ✓ Created + audited |
| 04-02 | Routes CRUD | Not started (depends on 04-01) |
| 04-03 | Route Stops Editor (drag-and-drop) | Not started (depends on 04-02) |

---

## Key Technical Context (for APPLY)

**Entity slice pattern** (identical to Phase 3):
- `src/entities/station/model/types.ts` — Station, StationInsert, StationUpdate, StationListParams
- `src/entities/station/api/station.api.ts` — 5 CRUD functions; `.or()` for name/city search; trim search before ilike
- `src/entities/station/api/station.queries.ts` — 5 TanStack Query hooks
- `src/entities/station/index.ts` — barrel

**Critical audit fixes to implement correctly:**
1. `mapSupabaseError` must include `details?: string` in param type; check `stations_name_key` not just `'name'`
2. Form reset: `useEffect([station, reset])` with explicit object literal for both null (create) and non-null (edit) branches — NO `serializeFormDefaults` function
3. lat/lng: `type="text"` inputs + `z.coerce.number()` (same as maintenance cost)
4. is_active: `<Controller>` with `checked={field.value}` + `onCheckedChange={field.onChange}`
5. Search: `const q = (search ?? '').trim(); if (q) { query.or(...) }`

**Station DB schema (from migration 20260411140000):**
- `name text NOT NULL UNIQUE` — constraint: `stations_name_key`
- `code text UNIQUE` — constraint: `stations_code_key`
- `city text NOT NULL`
- `latitude numeric(9,6)` — CHECK -90..90
- `longitude numeric(9,6)` — CHECK -180..180
- `is_active boolean DEFAULT true`
- DELETE RESTRICT from `routes.origin_station_id`, `routes.destination_station_id`, `route_stops.station_id`

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview |
| `.paul/phases/04-route-station-management/04-01-PLAN.md` | Stations CRUD plan (audited, ready for APPLY) |
| `.paul/phases/04-route-station-management/04-01-AUDIT.md` | Full audit report |
| `src/pages/vehicles/model/vehicle-form-schema.ts` | Reference for mapSupabaseError + serializeToInsert patterns |
| `src/entities/vehicle-type/api/vehicle-type.api.ts` | Reference for entity API pattern |
| `src/app/lib/router.tsx` | Add StationsPage import + replace PlaceholderPage |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:resume` or jump directly to `/paul:apply .paul/phases/04-route-station-management/04-01-PLAN.md`
3. Load required skills before implementing: `/feature-sliced-design` and `/frontend-design`

---

*Handoff created: 2026-04-15*
