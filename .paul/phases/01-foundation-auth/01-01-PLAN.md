---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - tailwind.config.ts
  - postcss.config.js
  - .eslintrc.cjs
  - .prettierrc
  - .env.local
  - .env.example
  - index.html
  - src/main.tsx
  - src/vite-env.d.ts
  - src/app/index.ts
  - src/app/lib/router.tsx
  - src/app/lib/query-client.ts
  - src/app/providers/index.ts
  - src/app/providers/app-providers.tsx
  - src/app/styles/index.css
  - src/app/app.tsx
  - src/shared/index.ts
  - src/shared/api/index.ts
  - src/shared/api/supabase-client.ts
  - src/shared/config/index.ts
  - src/shared/config/routes.ts
  - src/shared/lib/cn.ts
  - src/shared/lib/validate-env.ts
  - src/shared/lib/index.ts
  - src/shared/ui/error-boundary.tsx
  - src/shared/ui/index.ts
  - src/entities/index.ts
  - src/features/index.ts
  - src/widgets/index.ts
  - src/pages/index.ts
  - src/processes/index.ts
autonomous: true
---

<objective>
## Goal
Initialize the FleetGo Vite + React 18 + TypeScript 5 project with all tooling, create the Feature-Sliced Design v2.1 directory structure with path aliases and public API files, and set up the Supabase client with app entry point.

## Purpose
This is the foundational layer that every subsequent plan and phase depends on. Without proper scaffolding, FSD boundaries, and Supabase connectivity, no feature work can begin. Getting this right ensures architectural consistency across the entire project.

## Output
- Fully configured Vite project with all dependencies installed
- Complete FSD v2.1 directory structure with path aliases
- Supabase client ready for auth and data operations
- App entry point with providers (QueryClient, Router)
- Dev server starts without errors
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
This is a greenfield project — no source files exist yet.
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /feature-sliced-design | required | Before structuring FSD layers, creating directory structure, configuring path aliases | ○ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.
Run each skill command or confirm already loaded.

## Skill Invocation Checklist
- [ ] /feature-sliced-design loaded (run command or confirm)

</skills>

<acceptance_criteria>

## AC-1: Project Tooling Configured
```gherkin
Given a clean project directory with PAUL config files
When "npm install" and "npm run dev" are executed
Then the Vite dev server starts without errors and displays a React app at localhost:5173
And TypeScript strict mode is enabled
And TailwindCSS processes utility classes
And ESLint + Prettier are configured
```

## AC-2: FSD v2.1 Directory Structure Established
```gherkin
Given the project is initialized
When examining the src/ directory
Then all 7 FSD layers exist (app, processes, pages, widgets, features, entities, shared)
And each layer has an index.ts public API file
And path aliases (@app, @processes, @pages, @widgets, @features, @entities, @shared) resolve in both Vite and TypeScript
And importing from a layer via its public API works (e.g., import { } from "@shared/api")
```

## AC-3: Supabase Client & App Entry Point Functional
```gherkin
Given the project structure is in place
When the app loads
Then the Supabase client is initialized with environment variables from .env.local
And QueryClientProvider wraps the app (TanStack Query v5)
And BrowserRouter wraps the app (React Router v6)
And a root route renders without errors
```

## AC-4: Environment Validation & Error Recovery <!-- audit-added -->
```gherkin
Given the app is starting up
When VITE_SUPABASE_URL or VITE_SUPABASE_ANON_KEY is missing or empty
Then the app displays a clear error message identifying which variable(s) are missing
And the app does NOT proceed to render with an invalid Supabase client

Given the app is running in production
When a provider-level render error occurs (QueryClientProvider, RouterProvider)
Then an error boundary catches the error and displays a recovery UI instead of a blank white screen
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite project with all tooling and dependencies</name>
  <files>
    package.json, vite.config.ts, tsconfig.json, tsconfig.node.json,
    tailwind.config.ts, postcss.config.js, .eslintrc.cjs, .prettierrc,
    .env.local, .env.example, index.html
  </files>
  <action>
    1. Initialize a Vite 5 project with React 18 + TypeScript 5 template in the current directory (do NOT create a subdirectory — initialize in-place):
       ```bash
       npm create vite@latest . -- --template react-ts
       ```
       If the command fails due to existing files, use `--force` flag or manually create the files.

    2. Install all production dependencies:
       ```bash
       npm install react@18 react-dom@18 react-router-dom@6 @tanstack/react-query@5 @supabase/supabase-js@2 react-hook-form @hookform/resolvers zod dayjs @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
       ```

    3. Install all dev dependencies:
       ```bash
       npm install -D tailwindcss@3 postcss autoprefixer @types/react@18 @types/react-dom@18 eslint prettier eslint-config-prettier eslint-plugin-react-hooks @tailwindcss/typography class-variance-authority clsx tailwind-merge lucide-react tailwindcss-animate
       ```

    4. Configure vite.config.ts:
       - Set up path aliases for all FSD layers mapping to src/ subdirectories:
         - @app → src/app
         - @processes → src/processes
         - @pages → src/pages
         - @widgets → src/widgets
         - @features → src/features
         - @entities → src/entities
         - @shared → src/shared
       - Use `resolve.alias` with `path.resolve` for each alias
       - Add React plugin

    5. Configure tsconfig.json:
       - Set "strict": true
       - Set "target": "ES2020", "module": "ESNext", "moduleResolution": "bundler"
       - Add path aliases matching Vite aliases under "paths":
         - "@app/*": ["src/app/*"]
         - "@processes/*": ["src/processes/*"]
         - "@pages/*": ["src/pages/*"]
         - "@widgets/*": ["src/widgets/*"]
         - "@features/*": ["src/features/*"]
         - "@entities/*": ["src/entities/*"]
         - "@shared/*": ["src/shared/*"]

    6. Configure tailwind.config.ts:
       - Set content paths: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"]
       - Enable darkMode: "class"
       - Add @tailwindcss/typography plugin
       - Configure shadcn/ui theme foundation (CSS variables for colors, border-radius, etc.) in the theme.extend section:
         - Colors using CSS variables (background, foreground, card, popover, primary, secondary, muted, accent, destructive, border, input, ring)
         - borderRadius mapping to CSS variables
       - Add tailwindcss-animate plugin

    7. Configure postcss.config.js with tailwindcss and autoprefixer plugins.

    8. Configure ESLint — use the format generated by the Vite template (likely eslint.config.js flat config format):
       - If Vite template generates `eslint.config.js`, modify it to add react-hooks plugin and prettier config
       - If legacy format, create `.eslintrc.cjs` extending react-ts recommended + react-hooks + prettier
       - Delete any conflicting ESLint config files (do not have both flat and legacy config)

    9. Configure .prettierrc:
       - semi: true
       - singleQuote: true
       - trailingComma: "all"
       - printWidth: 100
       - tabWidth: 2

    10. Create .env.example with placeholder:
        ```
        VITE_SUPABASE_URL=your-supabase-url
        VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
        ```

    11. Create .env.local with same placeholders (for local dev).

    12. Update index.html title to "FleetGo System".

    Avoid: Using deprecated packages, mixing CommonJS and ESM in configs, using any instead of strict typing.
  </action>
  <verify>
    1. Run "npm install" — exits with code 0
    2. Run "npm run build" — exits with code 0
    3. Run "npx tsc --noEmit" — no type errors
  </verify>
  <done>AC-1 satisfied: Vite dev server builds and TypeScript strict mode compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create FSD v2.1 directory structure with public APIs and path aliases</name>
  <files>
    src/app/index.ts, src/app/lib/router.tsx, src/app/lib/query-client.ts,
    src/app/providers/index.ts, src/app/providers/app-providers.tsx,
    src/app/styles/index.css, src/app/app.tsx,
    src/shared/index.ts, src/shared/api/index.ts, src/shared/api/supabase-client.ts,
    src/shared/config/index.ts, src/shared/config/routes.ts,
    src/shared/lib/index.ts, src/shared/ui/index.ts,
    src/entities/index.ts, src/features/index.ts, src/widgets/index.ts,
    src/pages/index.ts, src/processes/index.ts, src/main.tsx
  </files>
  <action>
    1. Create all FSD layer directories with their segments:

       src/app/
       ├── lib/          (app-level utilities: router config, query client)
       ├── providers/    (app-level providers)
       ├── styles/       (global CSS)
       └── index.ts      (public API)

       src/shared/
       ├── api/          (API clients: supabase client)
       ├── config/       (configuration: routes constants)
       ├── lib/          (utilities: cn, format helpers)
       ├── ui/           (shared UI components - empty for now)
       └── index.ts      (public API)

       src/entities/
       └── index.ts      (public API - entities will be added per-domain)

       src/features/
       └── index.ts      (public API)

       src/widgets/
       └── index.ts      (public API)

       src/pages/
       └── index.ts      (public API)

       src/processes/
       └── index.ts      (public API)

    2. Create public API files (index.ts) for each layer:
       - Each index.ts should start empty or export only what exists
       - Use comment: "// Public API - exports will be added as slices are created"

    3. Create src/app/styles/index.css with Tailwind directives and shadcn/ui CSS variables:
       ```css
       @tailwind base;
       @tailwind components;
       @tailwind utilities;

       @layer base {
         :root {
           --background: 0 0% 100%;
           --foreground: 222.2 84% 4.9%;
           --card: 0 0% 100%;
           --card-foreground: 222.2 84% 4.9%;
           --popover: 0 0% 100%;
           --popover-foreground: 222.2 84% 4.9%;
           --primary: 222.2 47.4% 11.2%;
           --primary-foreground: 210 40% 98%;
           --secondary: 210 40% 96.1%;
           --secondary-foreground: 222.2 47.4% 11.2%;
           --muted: 210 40% 96.1%;
           --muted-foreground: 215.4 16.3% 46.9%;
           --accent: 210 40% 96.1%;
           --accent-foreground: 222.2 47.4% 11.2%;
           --destructive: 0 84.2% 60.2%;
           --destructive-foreground: 210 40% 98%;
           --border: 214.3 31.8% 91.4%;
           --input: 214.3 31.8% 91.4%;
           --ring: 222.2 84% 4.9%;
           --radius: 0.5rem;
         }

         .dark {
           --background: 222.2 84% 4.9%;
           --foreground: 210 40% 98%;
           --card: 222.2 84% 4.9%;
           --card-foreground: 210 40% 98%;
           --popover: 222.2 84% 4.9%;
           --popover-foreground: 210 40% 98%;
           --primary: 210 40% 98%;
           --primary-foreground: 222.2 47.4% 11.2%;
           --secondary: 217.2 32.6% 17.5%;
           --secondary-foreground: 210 40% 98%;
           --muted: 217.2 32.6% 17.5%;
           --muted-foreground: 215 20.2% 65.1%;
           --accent: 217.2 32.6% 17.5%;
           --accent-foreground: 210 40% 98%;
           --destructive: 0 62.8% 30.6%;
           --destructive-foreground: 210 40% 98%;
           --border: 217.2 32.6% 17.5%;
           --input: 217.2 32.6% 17.5%;
           --ring: 212.7 26.8% 83.9%;
         }
       }

       @layer base {
         * { @apply border-border; }
         body { @apply bg-background text-foreground; }
       }
       ```

    4. Create src/shared/config/routes.ts with route constants:
       ```typescript
       export const ROUTES = {
         HOME: '/',
         LOGIN: '/login',
         DASHBOARD: '/dashboard',
         VEHICLES: '/vehicles',
         VEHICLE_TYPES: '/vehicle-types',
         MAINTENANCE: '/maintenance',
         ROUTES: '/routes',
         STATIONS: '/stations',
         EMPLOYEES: '/employees',
         ROLES: '/roles',
         TRIPS: '/trips',
         TRIP_CALENDAR: '/trips/calendar',
         MY_SCHEDULE: '/my-schedule',
         CUSTOMERS: '/customers',
         BOOKINGS: '/bookings',
         PAYMENTS: '/payments',
       } as const;
       ```

    5. Create src/shared/lib/cn.ts with the cn utility (classnames merger):
       ```typescript
       import { clsx, type ClassValue } from 'clsx';
       import { twMerge } from 'tailwind-merge';

       export function cn(...inputs: ClassValue[]) {
         return twMerge(clsx(inputs));
       }
       ```

    5b. <!-- audit-added --> Create src/shared/lib/validate-env.ts with environment variable validation:
       ```typescript
       const requiredEnvVars = ['VITE_SUPABASE_URL', 'VITE_SUPABASE_ANON_KEY'] as const;

       export function validateEnv(): void {
         const missing = requiredEnvVars.filter(
           (key) => !import.meta.env[key] || import.meta.env[key] === `your-${key.toLowerCase().replace(/_/g, '-')}`
         );

         if (missing.length > 0) {
           throw new Error(
             `Missing required environment variables: ${missing.join(', ')}. ` +
             `Check your .env.local file against .env.example.`
           );
         }
       }
       ```
       This function is called before the app renders. If env vars are missing or still set to placeholder values, it throws immediately with a clear message instead of silently creating a broken Supabase client.

    6. <!-- audit-added --> Create src/shared/ui/error-boundary.tsx with a minimal app-level error boundary:
       ```typescript
       import { Component, type ErrorInfo, type ReactNode } from 'react';

       interface Props {
         children: ReactNode;
       }

       interface State {
         hasError: boolean;
         error: Error | null;
       }

       export class ErrorBoundary extends Component<Props, State> {
         constructor(props: Props) {
           super(props);
           this.state = { hasError: false, error: null };
         }

         static getDerivedStateFromError(error: Error): State {
           return { hasError: true, error };
         }

         componentDidCatch(error: Error, errorInfo: ErrorInfo) {
           console.error('Application error:', error, errorInfo);
         }

         render() {
           if (this.state.hasError) {
             return (
               <div style={{ padding: '2rem', maxWidth: '600px', margin: '2rem auto', fontFamily: 'system-ui' }}>
                 <h1 style={{ color: '#dc2626' }}>Something went wrong</h1>
                 <p>The application encountered an unexpected error.</p>
                 <pre style={{ background: '#f5f5f5', padding: '1rem', borderRadius: '0.5rem', overflow: 'auto' }}>
                   {this.state.error?.message}
                 </pre>
                 <button
                   onClick={() => window.location.reload()}
                   style={{ marginTop: '1rem', padding: '0.5rem 1rem', cursor: 'pointer' }}
                 >
                   Reload page
                 </button>
               </div>
             );
           }
           return this.props.children;
         }
       }
       ```
       This catches provider-level render errors (QueryClient, Router) and prevents white screen of death.

    7. Create src/shared/api/supabase-client.ts:
       ```typescript
       import { createClient } from '@supabase/supabase-js';

       export const supabase = createClient(
         import.meta.env.VITE_SUPABASE_URL,
         import.meta.env.VITE_SUPABASE_ANON_KEY,
       );
       ```

    7. Create src/app/lib/query-client.ts:
       ```typescript
       import { QueryClient } from '@tanstack/react-query';

       export const queryClient = new QueryClient({
         defaultOptions: {
           queries: {
             staleTime: 5 * 60 * 1000,
             retry: 1,
           },
         },
       });
       ```

    8. Create src/app/lib/router.tsx with a minimal router:
       ```typescript
       import { createBrowserRouter } from 'react-router-dom';

       export const router = createBrowserRouter([
         {
           path: '/',
           element: <div>FleetGo System - Loading...</div>,
         },
       ]);
       ```

    9. Create src/app/providers/app-providers.tsx:
       ```typescript
       import { QueryClientProvider } from '@tanstack/react-query';
       import { RouterProvider } from 'react-router-dom';
       import { queryClient } from '../lib/query-client';
       import { router } from '../lib/router';

       export function AppProviders() {
         return (
           <QueryClientProvider client={queryClient}>
             <RouterProvider router={router} />
           </QueryClientProvider>
         );
       }
       ```

    10. Create src/app/app.tsx that renders AppProviders.

    11. <!-- audit-added --> Create src/main.tsx as entry point with env validation and error boundary:
        ```typescript
        import React from 'react';
        import ReactDOM from 'react-dom/client';
        import { App } from './app';
        import { validateEnv } from './shared/lib/validate-env';
        import { ErrorBoundary } from './shared/ui/error-boundary';
        import './app/styles/index.css';

        // Fail fast with clear message if env is misconfigured
        validateEnv();

        ReactDOM.createRoot(document.getElementById('root')!).render(
          <React.StrictMode>
            <ErrorBoundary>
              <App />
            </ErrorBoundary>
          </React.StrictMode>,
        );
        ```

    12. Update each layer's index.ts to export what exists:
        - src/shared/index.ts: export { supabase } from './api/supabase-client'; export { cn } from './lib/cn'; export { ROUTES } from './config/routes'; export { ErrorBoundary } from './ui/error-boundary';
        - src/shared/lib/index.ts: export { cn } from './cn'; export { validateEnv } from './validate-env';
        - src/app/index.ts: export { App } from './app';
        - src/app/providers/index.ts: export { AppProviders } from './app-providers';

    13. Remove any Vite template boilerplate (App.css, src/App.tsx, src/App.css, etc.) that conflicts with the FSD structure.

    Avoid: Creating slices within layers (those come per-plan as features are built). Creating unused files. Placing business logic in shared layer.
  </action>
  <verify>
    1. Run "npm run build" — exits with code 0
    2. Run "npx tsc --noEmit" — no type errors
    3. Verify path alias resolution: create a test import like "import { cn } from '@shared/lib/cn'" and confirm no errors
    4. Run "npm run dev" — dev server starts and renders "FleetGo System - Loading..." at localhost:5173
    5. <!-- audit-added --> Verify env validation: temporarily remove VITE_SUPABASE_URL from .env.local, run dev, confirm a clear error message appears (not a cryptic Supabase error). Restore .env.local after.
    6. <!-- audit-added --> Confirm both light and dark mode CSS variables are complete (no placeholder comments in .dark block)
  </verify>
  <done>AC-2 satisfied: All 7 FSD layers exist with public APIs and working path aliases. AC-3 satisfied: Supabase client initialized, QueryClientProvider and BrowserRouter wrap the app, root route renders without errors. AC-4 satisfied: Env validation fails fast with clear message, ErrorBoundary wraps the app.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full integration and clean up</name>
  <files>
    .gitignore, src/vite-env.d.ts
  </files>
  <action>
    1. Update .gitignore to include:
       - node_modules/
       - dist/
       - .env.local
       - .env.*.local
       - *.log

    2. Ensure src/vite-env.d.ts exists with:
       ```typescript
       /// <reference types="vite/client" />

       interface ImportMetaEnv {
         readonly VITE_SUPABASE_URL: string;
         readonly VITE_SUPABASE_ANON_KEY: string;
       }

       interface ImportMeta {
         readonly env: ImportMetaEnv;
       }
       ```

    3. Delete all Vite template boilerplate that is NOT part of the FSD structure:
       - Remove src/App.tsx, src/App.css, src/assets/ if they exist from Vite template
       - Keep only the FSD structure under src/

    4. Run full verification suite:
       - npm run build (production build succeeds)
       - npx tsc --noEmit (type checking passes)
       - npm run lint (linting passes)
       - npm run dev (dev server starts)

    5. Verify Supabase client can be imported without runtime errors (it should initialize even with placeholder env vars — it only fails on actual API calls).

    Avoid: Committing any changes (user controls commits). Leaving template boilerplate that could confuse future development.
  </action>
  <verify>
    1. "npm run build" exits with code 0
    2. "npx tsc --noEmit" exits with code 0
    3. "npm run lint" exits with code 0 (or only warnings, no errors)
    4. "npm run dev" starts and serves the app at localhost:5173
    5. No Vite template boilerplate files remain (App.tsx, App.css, assets/ removed)
  </verify>
  <done>AC-1, AC-2, AC-3 fully satisfied: Project builds, type-checks, lints, and runs with complete FSD structure and Supabase client.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- .paul/ directory (PAUL framework files)
- .claude/ directory (Claude Code configuration)
- .agents/ directory (agent skills)
- skills-lock.json

## SCOPE LIMITS
- No Shadcn/ui component installation yet (Plan 01-02)
- No auth implementation yet (Plan 01-03)
- No app layout or navigation yet (Plan 01-04)
- No database schema or migrations
- No business logic or domain entities
- No API routes or server-side code

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm install` completes without errors
- [ ] `npm run build` exits with code 0
- [ ] `npx tsc --noEmit` passes with no type errors
- [ ] `npm run lint` passes (no errors)
- [ ] `npm run dev` starts and serves app at localhost:5173
- [ ] All 7 FSD layer directories exist under src/
- [ ] Each layer has index.ts public API file
- [ ] Path aliases (@app, @shared, etc.) resolve in TypeScript
- [ ] Supabase client exports without errors
- [ ] No Vite template boilerplate remains
- [ ] <!-- audit-added --> Dark mode CSS variables are complete (all variables have both :root and .dark values)
- [ ] <!-- audit-added --> Env validation catches missing/placeholder env vars with clear error message
- [ ] <!-- audit-added --> ErrorBoundary wraps the app and catches provider-level errors
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Dev server starts with FSD-structured React app
- Supabase client initialized and importable
- Path aliases resolve for all 7 FSD layers
- No TypeScript, ESLint, or build errors
</success_criteria>

<output>
After completion, create `.paul/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
