---
phase: 13-toast-message-standardization
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/pages/maintenance/ui/maintenance-form-page.tsx
  - src/pages/trips/ui/trip-form-page.tsx
  - src/pages/routes/ui/route-form-page.tsx
  - src/pages/routes/ui/route-stops-page.tsx
  - src/pages/employees/ui/employee-form-page.tsx
  - src/pages/bookings/ui/booking-form-page.tsx
  - src/pages/bookings/ui/booking-detail-dialog.tsx
  - src/pages/check-in/ui/check-in-page.tsx
  - src/pages/payments/ui/payment-status-dialog.tsx
autonomous: false
---

<objective>
## Goal
Standardize all toast messages in full-page forms and special pages to use the same consistent 3-field structure established in Plan 13-01.

## Purpose
Continuation of Phase 13. Plan 13-01 covered dialogs. This plan covers full-page forms (maintenance, trip, route, employee, booking) and special pages (check-in, payment, booking-detail). After this plan, all 26 non-conforming files will be standardized.

## Output
9 page/mixed files updated. Phase 13 fully complete.
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Standard Toast Pattern (SAME AS 13-01)

**Success:**
```tsx
toast({ title: 'Thành công', description: '[what happened in Vietnamese]', variant: 'success' });
```

**Error:**
```tsx
toast({ title: 'Lỗi', description: mapXxxError(...), variant: 'destructive' });
```

**Warning (partial success):**
```tsx
toast({ title: 'Cảnh báo', description: '[what partially succeeded and what failed]', variant: 'destructive' });
```

## Already-standardized files (DO NOT TOUCH)
- `src/pages/trips/ui/staff-assignment-page.tsx` ✅ (title: 'Thành công' + description + variant already)
- `src/app/layouts/app-layout/ui/header.tsx` ✅ (domain-specific auth error title acceptable)
- `src/pages/login/ui/login-page.tsx` ✅ (domain-specific auth error title acceptable)

## Prior plan summary
Plan 13-01 standardized all 17 dialog files. See 13-01-SUMMARY.md.

## Source Files
@src/pages/maintenance/ui/maintenance-form-page.tsx
@src/pages/trips/ui/trip-form-page.tsx
@src/pages/routes/ui/route-form-page.tsx
@src/pages/routes/ui/route-stops-page.tsx
@src/pages/employees/ui/employee-form-page.tsx
@src/pages/bookings/ui/booking-form-page.tsx
@src/pages/bookings/ui/booking-detail-dialog.tsx
@src/pages/check-in/ui/check-in-page.tsx
@src/pages/payments/ui/payment-status-dialog.tsx
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /frontend-design | required | Before editing UI component files | ○ |

**BLOCKING:** `/frontend-design` MUST be loaded before APPLY proceeds.

## Skill Invocation Checklist
- [ ] /frontend-design loaded (run command or confirm already loaded)
</skills>

<acceptance_criteria>

## AC-1: Success toasts in pages have title + description + variant: 'success'
```gherkin
Given any full-page form or special page in the files list
When an operation completes successfully
Then the toast shows title: 'Thành công', a non-empty Vietnamese description, and variant: 'success'
```

## AC-2: Error toasts in pages have title + description + variant: 'destructive'
```gherkin
Given any full-page form or special page in the files list
When an operation fails
Then the toast shows title: 'Lỗi', a non-empty description from the error mapper, and variant: 'destructive'
```

## AC-3: Warning toasts use title: 'Cảnh báo'
```gherkin
Given employee-form-page where employee was saved but role assignment failed
When the partial-failure toast fires
Then it shows title: 'Cảnh báo', the warning text as description, and variant: 'destructive'
```

## AC-4: Validation toasts in check-in-page use title: 'Lỗi'
```gherkin
Given check-in-page where user submits empty booking code
When the validation toast fires
Then it shows title: 'Lỗi', description: 'Vui lòng nhập mã đặt vé', and variant: 'destructive'
```

## AC-5: No bare toast calls without variant remain in the 9 files
```gherkin
Given any of the 9 files
When searched for toast({ without a variant field
Then none exist
```

</acceptance_criteria>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Verify /frontend-design skill is loaded</name>
  <action>
    The /frontend-design skill MUST be loaded before proceeding with APPLY.
    Confirm by running `/frontend-design` command or verify it's already active.

    This skill is required for UI component modifications to ensure design consistency.
  </action>
  <verify>Type `/frontend-design` and confirm skill is loaded without errors</verify>
  <done>/frontend-design skill is available for UI work</done>
</task>

<task type="auto">
  <name>Task 1: Standardize maintenance, trip, and route form page toasts</name>
  <files>
    src/pages/maintenance/ui/maintenance-form-page.tsx,
    src/pages/trips/ui/trip-form-page.tsx,
    src/pages/routes/ui/route-form-page.tsx,
    src/pages/routes/ui/route-stops-page.tsx
  </files>
  <action>
    Read each file first. Then apply:

    **maintenance-form-page.tsx** (line ~140-155):
    - Success create: `{ title: 'Đã tạo lịch bảo trì' }` (no variant, no description) 
      → `{ title: 'Thành công', description: 'Đã tạo lịch bảo trì', variant: 'success' }`
    - Success update: `{ title: 'Đã cập nhật lịch bảo trì' }` (no variant, no description)
      → `{ title: 'Thành công', description: 'Đã cập nhật lịch bảo trì', variant: 'success' }`
    - Error toast: check if has `title: 'Lỗi'`; if missing → add it

    **trip-form-page.tsx** (line ~131-158):
    - Success update: `{ title: 'Đã cập nhật chuyến đi' }` (no variant, no description)
      → `{ title: 'Thành công', description: 'Đã cập nhật chuyến đi', variant: 'success' }`
    - Success create: `{ title: 'Đã tạo chuyến đi' }` (no variant, no description)
      → `{ title: 'Thành công', description: 'Đã tạo chuyến đi', variant: 'success' }`
    - Error toast: already has `title: 'Lỗi'` + description + destructive ✅ leave unchanged

    **route-form-page.tsx** (line ~132-158):
    - Success update: `{ title: 'Đã cập nhật tuyến đường' }` (no variant, no description)
      → `{ title: 'Thành công', description: 'Đã cập nhật tuyến đường', variant: 'success' }`
    - Success create: `{ title: 'Đã tạo tuyến đường' }` (no variant, no description)
      → `{ title: 'Thành công', description: 'Đã tạo tuyến đường', variant: 'success' }`
    - Error toast: check if has `title: 'Lỗi'`; if missing → add it

    **route-stops-page.tsx** (line ~238):
    - Success: `{ title: 'Đã lưu điểm dừng' }` (no variant, no description)
      → `{ title: 'Thành công', description: 'Đã lưu điểm dừng', variant: 'success' }`
    - Check if any error toast; fix if missing title/variant

    Avoid: changing any logic beyond toast() call parameters.
  </action>
  <verify>Grep `grep -A 3 "toast({"` in each file — every call has both `title:` and `variant:` fields (multi-line safe)</verify>
  <done>AC-1, AC-2, AC-5 satisfied for maintenance/trip/route pages</done>
</task>

<task type="auto">
  <name>Task 1.5: TypeScript compilation check</name>
  <action>
    After Task 1 changes, run TypeScript compiler to catch any type errors early.
  </action>
  <verify>Run `npx tsc --noEmit` — must pass with zero errors</verify>
  <done>TypeScript compilation passes</done>
</task>

<task type="auto">
  <name>Task 2: Standardize employee form page, booking form page, and booking detail toasts</name>
  <files>
    src/pages/employees/ui/employee-form-page.tsx,
    src/pages/bookings/ui/booking-form-page.tsx,
    src/pages/bookings/ui/booking-detail-dialog.tsx
  </files>
  <action>
    Read each file first. Then apply:

    **employee-form-page.tsx** (line ~191-207):
    - Warning toast (partial save): `{ title: 'Nhân viên đã được lưu, nhưng không thể cập nhật vai trò. Vui lòng thử lại.', variant: 'destructive' }`
      → `{ title: 'Cảnh báo', description: 'Nhân viên đã được lưu, nhưng không thể cập nhật vai trò. Vui lòng thử lại.', variant: 'destructive' }`
    - Success toast: `{ title: 'Tạo/Cập nhật nhân viên thành công', variant: 'success' }` (no description)
      → `{ title: 'Thành công', description: mode === 'edit' ? 'Cập nhật nhân viên thành công' : 'Tạo nhân viên thành công', variant: 'success' }`

    **booking-form-page.tsx** (line ~229-249):
    - Success: `{ description: 'Tạo đặt vé thành công! Mã đặt vé: ${...}', variant: 'success' }` (no title)
      → `{ title: 'Thành công', description: 'Tạo đặt vé thành công! Mã đặt vé: ${result.booking_code}', variant: 'success' }`
    - Error: `{ variant: 'destructive', description: errorMessage }` (no title)
      → `{ title: 'Lỗi', description: errorMessage, variant: 'destructive' }`

    **booking-detail-dialog.tsx** (line ~84-88):
    - Success: `{ description: 'Đã hủy đặt vé', variant: 'success' }` (no title)
      → `{ title: 'Thành công', description: 'Đã hủy đặt vé', variant: 'success' }`
    - Error: check if has `title: 'Lỗi'`; if missing → add it

    Avoid: changing logic outside toast() calls, or altering the booking_code interpolation beyond adding title.
  </action>
  <verify>Grep `grep -A 3 "toast({"` in each file — every call has both `title:` and `variant:` fields (multi-line safe)</verify>
  <done>AC-1, AC-2, AC-3 satisfied for employee/booking pages</done>
</task>

<task type="auto">
  <name>Task 2.5: TypeScript compilation check</name>
  <action>
    After Task 2 changes, run TypeScript compiler to catch any type errors early.
  </action>
  <verify>Run `npx tsc --noEmit` — must pass with zero errors</verify>
  <done>TypeScript compilation passes</done>
</task>

<task type="auto">
  <name>Task 3: Standardize check-in page and payment status dialog toasts</name>
  <files>
    src/pages/check-in/ui/check-in-page.tsx,
    src/pages/payments/ui/payment-status-dialog.tsx
  </files>
  <action>
    Read each file first. Then apply:

    **check-in-page.tsx** (line ~33-64):
    Four toast calls — all currently missing `title:`:
    - Validation toast: `{ variant: 'destructive', description: 'Vui lòng nhập mã đặt vé' }` 
      → `{ title: 'Lỗi', description: 'Vui lòng nhập mã đặt vé', variant: 'destructive' }`
    - Check-in success: `{ description: 'Check-in thành công', variant: 'success' }`
      → `{ title: 'Thành công', description: 'Check-in thành công', variant: 'success' }`
    - Check-in error: `{ variant: 'destructive', description: message }`
      → `{ title: 'Lỗi', description: message, variant: 'destructive' }`
    - Check-in all success: `{ description: 'Check-in tất cả vé thành công', variant: 'success' }`
      → `{ title: 'Thành công', description: 'Check-in tất cả vé thành công', variant: 'success' }`
    - Check-in all error: `{ variant: 'destructive', description: message }`
      → `{ title: 'Lỗi', description: message, variant: 'destructive' }`

    **payment-status-dialog.tsx** (line ~67-71):
    - Success: `{ description: 'Đã cập nhật trạng thái thanh toán', variant: 'success' }` (no title)
      → `{ title: 'Thành công', description: 'Đã cập nhật trạng thái thanh toán', variant: 'success' }`
    - Error: check if has `title: 'Lỗi'`; if missing → add it

    Avoid: changing check-in logic, mutation calls, or any code outside toast() parameters.
  </action>
  <verify>Grep `grep -A 3 "toast({"` in each file — every call has both `title:` and `variant:` fields (multi-line safe)</verify>
  <done>AC-1, AC-2, AC-4, AC-5 satisfied for check-in and payment pages</done>
</task>

<task type="auto">
  <name>Task 3.5: TypeScript compilation check</name>
  <action>
    After Task 3 changes, run TypeScript compiler to catch any type errors early.
  </action>
  <verify>Run `npx tsc --noEmit` — must pass with zero errors</verify>
  <done>TypeScript compilation passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All 9 page/mixed files updated with standardized toast messages. Phase 13 complete.</what-built>
  <how-to-verify>
    1. Run: `npm run build` — must pass with zero TypeScript errors
    2. Run: `npm run dev`
    3. Test maintenance form page — create a new maintenance log → toast shows "Thành công" title + description
    4. Test trip form page — edit a trip → toast shows "Thành công" title + description
    5. Test booking form page — create a booking → toast shows "Thành công" + booking code in description
    6. Test check-in page — submit empty booking code → toast shows "Lỗi" title
    7. Test check-in page — check in a ticket → toast shows "Thành công"
    8. Test payment status dialog — update payment status → toast shows "Thành công"
    9. Verify employee form page partial-save warning shows "Cảnh báo" title: Open browser DevTools → Network tab → set to "Offline" → save employee → should trigger role API failure
    10. Full grep verification: `grep -A 3 -r "toast({" src/pages/ src/app/ --include="*.tsx"` — confirm every match has both `title:` and `variant:` within 3 lines
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to UNIFY, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/shared/ui/toast.tsx` — toast component already correct
- `src/pages/trips/ui/staff-assignment-page.tsx` — already standardized ✅
- `src/app/layouts/app-layout/ui/header.tsx` — domain-specific auth title acceptable ✅
- `src/pages/login/ui/login-page.tsx` — domain-specific auth title acceptable ✅
- Any error mapper functions (mapTripError, mapBookingError, mapCheckInError, etc.)
- Form submission logic, mutation calls, navigation logic

## SCOPE LIMITS
- Only change toast() call parameters
- Do not add new toast calls
- Do not modify error message strings (only restructure into title/description fields)
- Dialog files are covered by Plan 13-01 — do not re-touch them

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes with zero errors
- [ ] Grep `grep -A 3 "toast({"` in all 9 files — every call has `title:` field
- [ ] Grep `grep -A 3 "toast({"` in all 9 files — every call has `variant:` field
- [ ] All error mappers (mapTripError, mapBookingError, etc.) called in toasts have default fallback returning 'Có lỗi xảy ra' for undefined/null inputs
- [ ] Human verification checkpoint approved
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 9 page files have standardized toast calls
- /frontend-design skill loaded and verified
- TypeScript compilation passes after each task group
- Success toasts: title 'Thành công' + description + variant 'success'
- Error toasts: title 'Lỗi' + description + variant 'destructive'
- Warning toasts: title 'Cảnh báo' + description + variant 'destructive'
- Build passes with zero errors
- Error mappers have fallback for undefined/null inputs
- Human verification checkpoint approved
- Phase 13 fully complete
</success_criteria>

<output>
After completion, create `.paul/phases/13-toast-message-standardization/13-02-SUMMARY.md`
</output>
