# PAUL Handoff

**Date:** 2026-04-15
**Status:** Paused — 04-02 loop closed, ready for 04-03 PLAN

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** FleetGo — Passenger coach fleet management system
**Core value:** Digitize and automate back-office operations (vehicles, routes, trips, ticketing) into a single source of truth for transportation companies.

---

## Current State

**Version:** v0.1 MVP (in progress)
**Phase:** 4 of 8 — Route & Station Management
**Plan:** 04-02 complete — ready to plan 04-03

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — ready for next PLAN]
```

---

## What Was Done This Session

- Resumed from HANDOFF (04-02 plan was created + audited, APPLY not started)
- Ran `/paul:apply` — 04-02 Routes CRUD shipped:
  - `@entities/route` entity slice (types, PostgREST FK join API, 5 TanStack Query hooks)
  - `/routes` list page (name search 300ms debounce + is_active filter, DataTable with station names)
  - Route form dialog (two station FK dropdowns from `useStations`, duration-as-minutes input, is_active Switch)
  - Route delete dialog (23503 trip-RESTRICT error mapping)
  - Router wired: `ROUTES.ROUTES` → `RoutesPage`
- Ran `/paul:unify` — loop closed, 04-02-SUMMARY.md created

---

## What's In Progress

Nothing. 04-02 loop is fully closed. Clean state.

---

## What's Next

**Immediate:** Run `/paul:plan` for 04-03 — Route Stops Editor
- Load required skills first: `/feature-sliced-design` and `/frontend-design`
- This plan will implement drag-and-drop intermediate stops using `@dnd-kit`

**After that:** Human-verify, then `/paul:unify` to close Phase 4

**Then:** Phase 4 complete → Phase 5 (Employee & Role Management)

---

## Phase 4 Plan Overview (3 plans total)

| Plan | Name | Status |
|------|------|--------|
| 04-01 | Stations CRUD | ✅ Complete |
| 04-02 | Routes CRUD | ✅ Complete |
| 04-03 | Route Stops Editor (drag-and-drop) | Not started — depends on 04-01 + 04-02 |

---

## Key Technical Context (for 04-03 PLAN)

**`@entities/route` public API (now available):**
- Types: `Route`, `RouteInsert`, `RouteUpdate`, `RouteListParams`
- Hooks: `useRoutes`, `useRoute`, `useCreateRoute`, `useUpdateRoute`, `useDeleteRoute`

**`@entities/station` public API (available):**
- Types: `Station`, `StationInsert`, `StationUpdate`, `StationListParams`
- Hooks: `useStations`, `useStation`, `useCreateStation`, `useUpdateStation`, `useDeleteStation`

**Route Stops DB schema (migration 20260411140000):**
- `route_stops(id, route_id, station_id, stop_order int NOT NULL, distance_from_origin numeric, arrival_time interval)`
- FK: `route_stops.route_id → routes(id) CASCADE DELETE`
- FK: `route_stops.station_id → stations(id) RESTRICT`
- UNIQUE: `(route_id, stop_order)` — no duplicate positions
- UNIQUE: `(route_id, station_id)` — no duplicate stations per route

**Tech already in place:**
- `@dnd-kit` installed (listed in package.json/tech stack)
- `Switch` component in `@shared/ui`
- `FK_DROPDOWN_PAGE_SIZE = 1000` pattern established in route-form-schema.ts

**mapSupabaseError context pattern (established in 04-02):**
- `mapSupabaseError(error, context?: 'mutate' | 'delete')` — reuse this signature for 04-03

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview |
| `.paul/phases/04-route-station-management/04-02-SUMMARY.md` | 04-02 completion summary |
| `src/entities/route/index.ts` | Route public API |
| `src/entities/station/index.ts` | Station public API for FK dropdowns |
| `src/pages/routes/ui/routes-page.tsx` | Routes list page (reference for 04-03 integration) |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:resume` to restore context
3. Load required skills: `/feature-sliced-design` then `/frontend-design`
4. Run `/paul:plan` for 04-03 — Route Stops Editor

---

*Handoff created: 2026-04-15*
