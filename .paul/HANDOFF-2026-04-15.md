# PAUL Handoff

**Date:** 2026-04-15
**Status:** paused — clean stop after 05-01 loop closed

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** FleetGo — passenger coach fleet and transportation management system
**Core value:** Digitize and automate fragmented coach company operations into a single source of truth (vehicles, routes, employees, trips, bookings, payments)

---

## Current State

**Version:** v0.1 MVP (in progress)
**Phase:** 5 of 8 — Employee & Role Management
**Plan:** 05-01 COMPLETE — 05-02 (Employees) is next

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [05-01 loop closed — IDLE, ready for 05-02]
```

---

## What Was Done This Session

- **05-01 APPLY executed** — all tasks PASS, human checkpoint approved:
  - `@entities/role` public API: useRoles, useRole, useCreateRole, useUpdateRole, useDeleteRole
  - `/roles` CRUD page: paginated list, name search (debounced 300ms), create/edit dialog with permissions chip editor, delete dialog with cascade warning
  - Router wired: ROUTES.ROLES → RolesPage (replaced PlaceholderPage)
- **05-01 UNIFY complete** — SUMMARY.md created, STATE.md + paul.json updated

---

## What's In Progress

Nothing — clean stop. 05-01 loop fully closed.

---

## What's Next

**Immediate:** `/paul:plan` for 05-02 (Employees CRUD)

**Key context for 05-02 planning:**
- `@entities/role` (useRoles) is now available for the role assignment dropdown
- DB: `employees` table — `user_id` FK → `profiles`, `license_number`, `license_expiry`, `phone`, `hire_date`, `is_active`
- `user_roles` junction table — `(user_id, role_id)` composite PK
- Feature: license expiry badges — "Sắp hết hạn" ≤30 days, "Hết hạn" if past
- Error map to plan: `employees_license_number_key` (23505), `employees_user_id_key` (23505), trip_staff FK RESTRICT (23503 on delete)
- 05-02 needs audit before APPLY (same as every plan)

**After that:** Phase 5 complete → transition → Phase 6 (Trip Scheduling)

---

## Key Patterns Established This Session

- **Permissions chip editor**: `useState<string>` for input + `useWatch` on permissions field + `setValue('permissions', [...])` — no external lib
- **Dialog submit error as React state** (not toast): `setSubmitError(mapRoleError(error))` below `<DialogFooter>` — dialog stays open on error
- **mapRoleError 23505**: checks `details?.includes('(name)')` — more reliable than `msg.includes` (MH audit fix)

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview |
| `.paul/phases/05-employee-role-management/05-02-PLAN.md` | Next plan to execute (needs audit first) |
| `.paul/phases/05-employee-role-management/05-01-SUMMARY.md` | What 05-01 built — context for 05-02 |
| `src/entities/role/index.ts` | @entities/role public API (needed by 05-02 form) |
| `src/pages/roles/ui/role-form-dialog.tsx` | Chip editor pattern reference |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:resume` or `/paul:progress` for orientation
3. Run `/paul:plan` — it will target 05-02 (Employees)
4. Before APPLY: load `/frontend-design` + `/feature-sliced-design` (required by SPECIAL-FLOWS.md)

---

*Handoff created: 2026-04-15*
